[build]
builder = "NIXPACKS"

[deploy]
startCommand = "pnpm --filter @prospector/api prisma:deploy && pnpm --filter @prospector/api start:prod"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
